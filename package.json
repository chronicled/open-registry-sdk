{
  "name": "open-registry-sdk",
  "version": "0.0.4",
  "description": "",
  "main": "lib/provider.js",
  "scripts": {
    "integration": "mocha integration/index.js -u tdd --timeout 0",
    "integration-configure": "mocha integration/configure.js -u tdd --timeout 0 --reporter spec",
    "integration-schema": "mocha integration/schema.js -u tdd --timeout 0 --reporter spec",
    "integration-registrant": "mocha integration/registrant.js -u tdd --timeout 0 --reporter spec",
    "integration-thing": "mocha integration/thing.js -u tdd --timeout 0 --reporter spec",
    "integration-consumer": "mocha integration/consumer.js -u tdd --timeout 0 --reporter spec"
  },
  "author": "Johann Barbie <johann@ambisafe.co> (http://ambisafe.co/)",
  "dependencies": {
    "async": "^1.5.2",
    "bigi": "^1.4.2",
    "bignumber.js": "^2.3.0",
    "bitcore-lib": "0.13.16",
    "bytebuffer": "^5.0.1",
    "es6-promise": "^3.1.2",
    "eth-lightwallet": "^2.4.3",
    "object-assign": "^4.1.0",
    "protobufjs": "^5.0.1",
    "web3": "^0.17.0-alpha",
    "web3-provider-engine": "^7.7.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.5.3",
    "sinon": "^1.17.4",
    "sinon-chai": "^2.8.0"
  }
}
