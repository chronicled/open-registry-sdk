{
  "name": "open-registry-sdk",
  "version": "0.0.3",
  "description": "",
  "main": "lib/provider.js",
  "scripts": {
    "test": "mocha test/index.js -u tdd --timeout 0",
    "test-configure": "mocha test/configure.js -u tdd --timeout 0 --reporter spec",
    "test-schema": "mocha test/schema.js -u tdd --timeout 0 --reporter spec",
    "test-registrant": "mocha test/registrant.js -u tdd --timeout 0 --reporter spec",
    "test-thing": "mocha test/thing.js -u tdd --timeout 0 --reporter spec",
    "test-consumer": "mocha test/consumer.js -u tdd --timeout 0 --reporter spec"
  },
  "author": "Johann Barbie <johann@ambisafe.co> (http://ambisafe.co/)",
  "dependencies": {
    "async": "^1.5.2",
    "bignumber.js": "^2.3.0",
    "bitcore-lib": "0.13.16",
    "bytebuffer": "^5.0.1",
    "es6-promise": "^3.1.2",
    "eth-lightwallet": "^2.4.3",
    "object-assign": "^4.1.0",
    "protobufjs": "^5.0.1",
    "web3": "^0.17.0-alpha",
    "web3-provider-engine": "^7.7.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.5.3",
    "sinon": "^1.17.4",
    "sinon-chai": "^2.8.0"
  }
}
